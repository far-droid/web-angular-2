import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wisata_garut_uts',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
